package com.codigovermelho.models.enums;

/**
 * Nivel de urgencia de uma requisicao hospitalar (US04).
 * Ja deixamos pronto aqui porque Requisicao (US04) vai depender deste enum.
 */
public enum NivelUrgencia {
    ROTINA,
    PRIORITARIA,
    EMERGENCIA
}
